spring:
  application:
    name: "App WSR"
  main:
    web-application-type: none
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1/wsr
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true